import { LtlMaskDirective } from './ltl-mask.directive';

describe('LtlMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new LtlMaskDirective();
    expect(directive).toBeTruthy();
  });
});
